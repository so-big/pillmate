Hash รหัสผ่าน, session ปลอดภัย, อ่านแจ้งเตือนจาก SQLite และปรับปรุงโครงสร้างข้อมูล

## สรุปการแก้ไข (11-02-2026) ✅
- **Security:** เปลี่ยนวิธีเก็บ/ตรวจสอบรหัสผ่านเป็น SHA-256 และย้ายการเก็บ session ไปไว้ที่ `flutter_secure_storage` (ไม่เก็บรหัสผ่านเป็น plaintext)
- **Database & Models:** สร้าง/ปรับปรุง models และ CRUD ใน `database_helper.dart` (เพิ่มตาราง `app_settings`, `calendar_alerts`, `taken_doses`) พร้อมรองรับการ migration (auto-hash รหัสผ่านจาก plaintext)
- **Notifications:** แก้ `notification_next.dart` ให้อ่านข้อมูลเตือนจาก SQLite (`calendar_alerts` / `taken_doses`) แทนการอ่านจาก `reminders.json` / `eated.json` และอ่านการตั้งค่าจาก `app_settings` (มี fallback เป็นไฟล์ JSON เดิม)
- **State & Services:** เพิ่ม `flutter_riverpod` providers (`lib/providers/providers.dart`) และไฟล์บริการ `lib/services/auth_service.dart` สำหรับ hashing/verification และ session management
- **Passwords in UI:** ปรับทุกหน้าที่เปรียบเทียบหรือบันทึกรหัสผ่านให้ใช้ `AuthService.hashPassword()` / `AuthService.verifyPassword()` (อาทิ `create_account.dart`, `edit_account.dart`, `forgot_password.dart`, `edit_calendar.dart`, `view_dashboard.dart`, `edit_medicine.dart`, `edit_profile.dart`, `manage_profile.dart`)
- **Renames & Imports:** แก้ไขไฟล์สะกดผิด (เช่น `nortification_*` → `notification_*`, `*_carlendar` → `*_calendar`) และอัปเดต import ทั้งโปรเจกต์
- **Misc & Cleanup:** ปรับ `.gitignore` (ไม่เก็บไฟล์ฐานข้อมูล/asset ส่วนตัว), ลบ/ปรับ unused imports บางรายการ, แก้ timezone ใน notification เป็น `FlutterTimezone.getLocalTimezone()`
- **Dev tasks executed:** `flutter pub get` (dependencies resolved, updated `intl` constraint), `flutter analyze` (0 errors, มี warnings/info ที่เป็นเรื่อง non-critical)

---

### รายการไฟล์ที่แก้/เพิ่มสำคัญ (ไม่ครบถ้วนเชิงเทคนิคแต่ครอบคลุม)
- เพิ่ม: `lib/models/*.dart`, `lib/services/auth_service.dart`, `lib/providers/providers.dart`
- แก้: `lib/database_helper.dart` (migration + CRUD), `lib/main.dart`, `lib/create_account.dart`, `lib/edit_account.dart`, `lib/forgot_password.dart`, `lib/edit_calendar.dart`, `lib/edit_medicine.dart`, `lib/edit_profile.dart`, `lib/manage_profile.dart`, `lib/notification_next.dart`, `lib/notification_setting.dart`, `lib/view_dashboard.dart`
- อื่นๆ: `.gitignore`, `pubspec.yaml` (dependencies), `commit_11-02-2026.md` (ไฟล์นี้)

> หากต้องการ ฉันสามารถสร้าง commit message และสร้าง commit/Git branch ให้ได้ต่อ (หรือปรับข้อความสรุปให้เป็นภาษาอังกฤษ/short form สำหรับ Github PR) ✨
