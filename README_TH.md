# 💊 Pillmate — แอปเตือนกินยาอัจฉริยะ พร้อมระบบติดตามการรับประทานยา

> แอปพลิเคชัน Flutter สำหรับช่วยผู้ป่วยและผู้ดูแลจัดการตารางการกินยา ยืนยันการรับประทานยาผ่านการสแกนแท็ก NFC และรับการแจ้งเตือนเสียงแบบปรับแต่งได้ — เพื่อไม่ให้พลาดการกินยาแม้แต่ครั้งเดียว

---

## 📖 ทำไมถึงต้องมีแอปนี้ (คุณค่าทางธุรกิจ)

### ปัญหา
การไม่รับประทานยาตามที่แพทย์สั่งเป็นวิกฤตด้านสาธารณสุขระดับโลก องค์การอนามัยโลก (WHO) ประมาณการว่า **ผู้ป่วยโรคเรื้อรังกว่า 50%** ไม่ได้รับประทานยาตามที่กำหนด ส่งผลให้โรคลุกลาม ต้องเข้ารักษาตัวในโรงพยาบาล และเสียชีวิตโดยไม่จำเป็น กลุ่มที่ได้รับผลกระทบมากที่สุดคือผู้สูงอายุ ผู้ป่วยที่ต้องกินยาหลายชนิด และผู้ดูแลที่ต้องจัดการยาให้ผู้อื่น

### วิธีแก้ปัญหา
**Pillmate** สร้างระบบป้องกันหลายชั้น:

1.  **การเตือนตามกำหนดเวลา:** แจ้งเตือนตามช่วงเวลาที่ตั้งไว้ ดังต่อเนื่องจนกว่าจะตอบรับ
2.  **การยืนยันด้วย NFC:** โหมดเสริมที่ผู้ป่วยต้องสแกนแท็ก NFC ที่ติดไว้กับกล่องยาเพื่อยืนยันว่ากินยาถูกตัว ป้องกันการ "ปิดเสียงเตือนแล้วลืม"
3.  **การจัดการหลายโปรไฟล์:** ผู้ใช้หลัก (Master) เช่น ผู้ดูแล สามารถสร้างและจัดการตารางยาให้ผู้พึ่งพิงหลายคน (เช่น พ่อแม่ผู้สูงอายุ ลูกหลาน)
4.  **ติดตามประวัติการกินยา:** มุมมองปฏิทินและหน้าแดชบอร์ดรายวัน แสดงให้เห็นว่าโดสไหนกินแล้ว โดสไหนพลาด

### กลุ่มเป้าหมาย
- ผู้ป่วยสูงอายุ หรือผู้ดูแลผู้สูงอายุ
- ผู้ป่วยโรคเรื้อรังที่ต้องรับประทานยาหลายชนิด
- ครอบครัวที่ต้องจัดการยาให้สมาชิกหลายคน
- ผู้ใช้ที่ใช้ภาษาไทย (UI ทั้งหมดเป็นภาษาไทย, locale `th_TH`)

---

## 🏗️ สถาปัตยกรรมปัจจุบัน

### เทคโนโลยีที่ใช้

| ชั้น               | เทคโนโลยี                                                               |
|--------------------|--------------------------------------------------------------------------|
| **Framework**      | Flutter (Dart) — รองรับหลายแพลตฟอร์ม (Android, iOS, Linux, Windows, Web) |
| **ฐานข้อมูลท้องถิ่น** | SQLite ผ่าน `sqflite` package ([`DatabaseHelper`](lib/database_helper.dart)) |
| **พื้นที่จัดเก็บ**  | ไฟล์ JSON บนดิสก์ (`path_provider`) สำหรับการตั้งค่า, สถานะ session, และบันทึกการแจ้งเตือน |
| **NFC**            | `flutter_nfc_kit` + `ndef` packages                                      |
| **การแจ้งเตือน**    | `flutter_local_notifications` + `flutter_timezone` + `timezone`          |
| **จัดการรูปภาพ**    | `image_picker`, เข้ารหัส/ถอดรหัส Base64 ด้วยตนเอง, ใช้ `dart:ui` canvas |
| **State Mgmt**     | `setState` (StatefulWidget แบบพื้นฐาน) — ไม่ใช้ไลบรารีจัดการ state ภายนอก |
| **Backend**        | **ไม่มี** แอปทำงานแบบออฟไลน์ทั้งหมดบนเครื่อง                            |

### การไหลของข้อมูลและ Logic

```
┌─────────────────────────────────────────────────────────┐
│                     ชั้น UI                              │
│  main.dart (เข้าสู่ระบบ) → view_dashboard.dart (ศูนย์กลาง) │
│  ├── view_carlendar.dart (มุมมองปฏิทินรายสัปดาห์)         │
│  ├── add_carlendar.dart (สร้างการเตือน + เขียน NFC)       │
│  ├── edit_carlendar.dart (แก้ไขการเตือน + เขียน NFC ใหม่)  │
│  ├── add_medicine.dart / edit_medicine.dart / manage_*    │
│  ├── create_profile.dart / edit_Profile.dart / manage_*   │
│  ├── nortification_setting.dart (ตั้งค่าเสียง & เลื่อนปลุก) │
│  └── view_menu.dart (เมนูลิ้นชัก)                         │
├─────────────────────────────────────────────────────────┤
│                   ชั้นบริการ (Service)                    │
│  nortification_service.dart (หาโดสถัดไปในเบื้องหลัง)      │
│  nortification_next.dart (ตั้งเตือนล่วงหน้า 2 วัน)       │
│  database_helper.dart (SQLite singleton)                 │
├─────────────────────────────────────────────────────────┤
│                   ชั้นข้อมูล (Data)                       │
│  SQLite DB: users, medicines, calendar_alerts, eated     │
│  JSON Files: user-stat.json, appstatus.json,             │
│              nortification_setup.json                    │
│  Assets: รูปยา, อวาตาร์โปรไฟล์, เสียงเตือน               │
└─────────────────────────────────────────────────────────┘
```

### การตัดสินใจทางสถาปัตยกรรมที่สำคัญ
- **พื้นที่จัดเก็บแบบไฮบริด:** แอปใช้ SQLite เป็นฐานข้อมูลหลัก ([`DatabaseHelper`](lib/database_helper.dart)) แต่*ยังอ่าน/เขียน*ไฟล์ JSON ดิบลงดิสก์สำหรับการตั้งค่า (`appstatus.json`), การคงสถานะ session (`user-stat.json`), และบันทึกการตั้งเตือน ทำให้เกิดปัญหาข้อมูลมีแหล่งความจริงสองแหล่ง
- **ออฟไลน์เป็นหลัก:** **ไม่มีเซิร์ฟเวอร์ ไม่มี API ไม่มีการซิงค์บนคลาวด์** ข้อมูลทั้งหมดอยู่บนเครื่อง ถ้าเครื่องหาย ข้อมูลหายทั้งหมด
- **NFC เป็นการยืนยัน:** แท็ก NFC ถูกเขียนด้วย payload ที่มีโครงสร้าง (`ชื่อยา~รายละเอียด~e=flag~et=ช่วงเวลา~ชื่อโปรไฟล์`) และอ่านกลับมาเพื่อยืนยันว่าผู้ป่วยกินยาถูกตัว

---

## ✅ ฟีเจอร์หลัก (สิ่งที่ใช้งานได้ในปัจจุบัน)

| ฟีเจอร์ | ไฟล์ | คำอธิบาย |
|---|---|---|
| **การยืนยันตัวตน (ท้องถิ่น)** | [`main.dart`](lib/main.dart), [`create_account.dart`](lib/create_account.dart) | เข้าสู่ระบบด้วย username/password เก็บใน SQLite ท้องถิ่น มี "จดจำฉัน" ผ่านไฟล์ JSON มีคำถามกันลืมสำหรับกู้รหัสผ่าน |
| **หลายโปรไฟล์** | [`create_profile.dart`](lib/create_profile.dart), [`manage_profile.dart`](lib/manage_profile.dart), [`edit_Profile.dart`](lib/edit_Profile.dart) | ผู้ใช้หลักสร้างโปรไฟล์ย่อยพร้อมอวาตาร์ (จาก asset หรือกล้อง), ชื่อ, ข้อมูล, และเวลาอาหารเฉพาะโปรไฟล์ |
| **จัดการตัวยา** | [`add_medicine.dart`](lib/add_medicine.dart), [`edit_medicine.dart`](lib/edit_medicine.dart), [`manage_medicine.dart`](lib/manage_medicine.dart) | CRUD สำหรับยา พร้อมชื่อ รายละเอียด ก่อน/หลังอาหาร และรูปยา (จาก asset หรือแกลเลอรี พร้อมครอป/ปรับขนาด) |
| **ตั้งเตือนปฏิทิน** | [`add_carlendar.dart`](lib/add_carlendar.dart), [`edit_carlendar.dart`](lib/edit_carlendar.dart) | สร้างการเตือนตามช่วงเวลาที่เชื่อมยากับโปรไฟล์ พร้อมวันเริ่ม/สิ้นสุด และผูกแท็ก NFC |
| **แดชบอร์ด** | [`view_dashboard.dart`](lib/view_dashboard.dart) | มุมมองรายวันพร้อมตัวเลื่อนวันแนวนอน แสดงโดสทั้งหมดของวัน สถานะกินแล้ว/กำลังสแกน และปุ่มดำเนินการด่วน |
| **มุมมองปฏิทิน** | [`view_carlendar.dart`](lib/view_carlendar.dart) | ปฏิทินรายสัปดาห์แสดงยาที่กำหนดในแต่ละวัน |
| **NFC อ่าน/เขียน** | [`add_carlendar.dart`](lib/add_carlendar.dart), [`edit_carlendar.dart`](lib/edit_carlendar.dart), [`view_dashboard.dart`](lib/view_dashboard.dart) | เขียนข้อมูลยาลงแท็ก NFC ตอนสร้างการเตือน; อ่านและตรวจสอบแท็ก NFC ตอนยืนยันการกินยา |
| **การแจ้งเตือนท้องถิ่น** | [`nortification_service.dart`](lib/nortification_service.dart), [`nortification_next.dart`](lib/nortification_next.dart) | ตั้งเวลาแจ้งเตือนระดับ OS พร้อมเสียงปลุกที่กำหนดเอง และการเลื่อน/ย้ำเตือนที่ปรับแต่งได้ |
| **ตั้งค่าการแจ้งเตือน** | [`nortification_setting.dart`](lib/nortification_setting.dart) | เลือกเสียงปลุก ระยะเวลาเลื่อนเตือน (ต่ำสุด 2 นาที) และจำนวนครั้งย้ำเตือน (1–10 ครั้ง) |
| **ลืมรหัสผ่าน** | [`forgotPassword.dart`](lib/forgotPassword.dart) | ขั้นตอนกู้รหัสผ่านผ่านคำถามกันลืม |
| **แก้ไขบัญชี** | [`edit_account.dart`](lib/edit_account.dart) | แก้ไขอวาตาร์ รหัสผ่าน คำถามกันลืม และเวลาอาหารของแต่ละโปรไฟล์ |

---

## 🔧 การติดตั้งและตั้งค่า

### สิ่งที่ต้องมี
- Flutter SDK (stable channel, แนะนำ ≥ 3.x)
- Android Studio หรือ Xcode (สำหรับ emulator)
- อุปกรณ์ Android จริงที่มี NFC สำหรับทดสอบฟีเจอร์เต็มรูปแบบ

### ขั้นตอน

```bash
# 1. Clone repository
git clone <repository-url>
cd pillmate

# 2. ติดตั้ง dependencies
flutter pub get

# 3. ตรวจสอบว่า assets ถูกประกาศใน pubspec.yaml แล้ว
#    (รูปยา, อวาตาร์โปรไฟล์, เสียง, ไฟล์ seed ฐานข้อมูล)

# 4. รันบนอุปกรณ์หรือ emulator ที่เชื่อมต่ออยู่
flutter run

# 5. สำหรับ build APK แบบ release
flutter build apk --release
```

### หมายเหตุสำคัญ
- ฐานข้อมูล SQLite จะถูกสร้างจาก [`assets/db/`](assets/db/) เมื่อเปิดใช้งานครั้งแรก ดูที่ [`DatabaseHelper.initDatabase()`](lib/database_helper.dart)
- ไฟล์ตั้งค่า `appstatus.json` จะถูกคัดลอกจาก [`assets/db/appstatus.json`](assets/db/appstatus.json) เมื่อเปิดใช้งานครั้งแรก ดูที่ [`_initializeAppStatusFile()`](lib/main.dart)
- ฟีเจอร์ NFC ต้องใช้อุปกรณ์จริงที่มีฮาร์ดแวร์ NFC จะทำงานแบบ manual mode ได้อย่างราบรื่นถ้า NFC ถูกปิด

---

## ⚠️ การวิเคราะห์และข้อวิจารณ์

### 🔴 ธงแดง — ปัญหาร้ายแรง

#### 1. **เก็บรหัสผ่านแบบข้อความธรรมดา** — ความรุนแรง: วิกฤต
รหัสผ่านถูกเก็บ**แบบข้อความธรรมดา**ในฐานข้อมูล SQLite ไม่มีการ hash ไม่มี salt ไม่มีการเข้ารหัสใดๆ ทั้งสิ้น

```dart
// filepath: lib/edit_carlendar.dart (บรรทัด ~693)
if (user['password'] == inputPassword) {
  return true;
}
```

รูปแบบเดียวกันนี้พบใน [`manage_profile.dart`](lib/manage_profile.dart), [`view_dashboard.dart`](lib/view_dashboard.dart), และ [`edit_account.dart`](lib/edit_account.dart) รหัสผ่านยังถูกเขียนลงไฟล์ `user-stat.json` บนดิสก์แบบข้อความธรรมดาสำหรับฟีเจอร์ "จดจำฉัน" (ดูที่ [`main.dart`](lib/main.dart) บรรทัด ~240)

#### 2. **ฐานข้อมูลไม่มีการเข้ารหัส**
ไฟล์ฐานข้อมูล SQLite ถูกเก็บบน filesystem ของเครื่องโดยไม่เข้ารหัส อุปกรณ์ที่ถูก root หรือเครื่องมือดึงข้อมูลสำรองสามารถอ่านข้อมูลผู้ใช้ ยา และข้อมูลสุขภาพทั้งหมดได้

#### 3. **ข้อมูล Session บนดิสก์แบบข้อความธรรมดา**
ฟีเจอร์ "จดจำฉัน" เขียน `{ "username": "...", "password": "..." }` ลงไฟล์ JSON ดูที่ [`_saveUserStat()`](lib/main.dart)

#### 4. **Timezone ถูกฝังตายในโค้ด**
ระบบแจ้งเตือนฝัง `Asia/Bangkok` ตายตัวใน [`nortification_next.dart`](lib/nortification_next.dart) (บรรทัด ~108):
```dart
return 'Asia/Bangkok';
```
จะใช้งานไม่ได้สำหรับผู้ใช้ที่อยู่นอกเขตเวลา ICT

### 🟡 ข้อกังวลด้านสถาปัตยกรรม

#### 5. **แหล่งความจริงสองแหล่ง (Dual Source of Truth)**
ข้อมูลถูกแบ่งระหว่าง SQLite และไฟล์ JSON ดิบ การตั้งค่าการแจ้งเตือนอยู่ใน `appstatus.json` ข้อมูลการเตือนถูก query จาก SQLite แต่ [`nortification_next.dart`](lib/nortification_next.dart) อ่านจากไฟล์ `reminders.json` และ `eated.json` (บรรทัด 48–56) ทำให้เกิดบั๊กเรื่องการซิงค์ข้อมูล

#### 6. **ไม่มีระบบจัดการ State**
แอปทั้งหมดใช้ `setState` กับ class `StatefulWidget` ขนาดใหญ่ [`view_dashboard.dart`](lib/view_dashboard.dart) เป็นไฟล์เดียวที่มี **มากกว่า 1,200 บรรทัด** ซึ่งรวม UI, business logic, การสแกน NFC, การอ่าน/เขียนไฟล์, การ query ฐานข้อมูล, และการตั้งเวลาแจ้งเตือนเข้าด้วยกันทั้งหมด

#### 7. **ไม่มีการแยกส่วนความรับผิดชอบ (Separation of Concerns)**
ไม่มี models, repositories, services (นอกจากไฟล์ notification), dependency injection ใดๆ Business logic ถูกฝังโดยตรงในเมธอด `build()` ของ widget และ handler `onPressed`

#### 8. **การตั้งชื่อไม่สม่ำเสมอ**
- `nortification_*` (สะกดคำว่า "notification" ผิดทั่วทั้ง codebase)
- `edit_Profile.dart` (P ตัวใหญ่) vs `edit_medicine.dart` (ตัวเล็ก)
- `add_carlendar.dart` (สะกดคำว่า "calendar" ผิด)
- ชื่อ class `CarlendarEditSheet`

#### 9. **ไม่มี Test**
โฟลเดอร์ `test/` มีอยู่แต่ไม่มีไฟล์ test ที่มีความหมาย ไม่มี unit test, widget test, หรือ integration test เลย

#### 10. **ไม่มีระบบจัดการ Error / รายงาน Crash**
Error ถูกจับด้วย `try/catch` แล้ว `debugPrint` อย่างเงียบๆ ไม่มีระบบรายงาน crash (Sentry, Firebase Crashlytics ฯลฯ)

#### 11. **การจัดการรูปภาพที่บวม**
รูปโปรไฟล์และรูปยาถูกเก็บเป็น **string Base64 เต็มๆ** ในฐานข้อมูล SQLite ซึ่งจะทำให้ประสิทธิภาพลดลงอย่างรุนแรงเมื่อฐานข้อมูลขยายใหญ่ขึ้น เพราะทุก query ที่แตะตารางเหล่านี้จะดึง blob Base64 ขนาดใหญ่เข้ามาในหน่วยความจำ

#### 12. **ไม่มีระบบ Localization**
แม้จะเป็นภาษาไทยทั้งหมด แต่แอปใช้ **string ภาษาไทยที่ฝังตายในโค้ด** ทุกที่ ไม่มี `intl`, ไม่มีไฟล์ `.arb`, ไม่มีโครงสร้าง localization ใดๆ การเพิ่มภาษาอังกฤษหรือภาษาอื่นต้องแก้ไขทุกไฟล์

#### 13. **การ Parse NFC Payload ด้วยมือ**
NFC payload ใช้รูปแบบ tilde-delimited แบบกำหนดเอง (`ชื่อ~รายละเอียด~e=flag~et=ช่วงเวลา~โปรไฟล์`) ที่ถูก parse ด้วยการ split string ไม่มีการตรวจสอบ schema ไม่มีการกำหนดเวอร์ชัน

### 🟢 สิ่งที่ทำได้ดี
- **ขอบเขตฟีเจอร์ทะเยอทะยานและครบถ้วน** สำหรับโปรเจกต์ระดับนักศึกษา/ส่วนตัว การยืนยันด้วย NFC เป็นไอเดียที่สร้างสรรค์จริงๆ
- **UI มีความสม่ำเสมอ** ด้วยธีมสีเขียว/teal สไตล์การแพทย์ และตัวอักษรภาษาไทยที่เหมาะสม
- **NFC Degradation แบบราบรื่น** — แอปทำงานในโหมด "manual" ได้เมื่อ NFC ถูกปิด
- [`DatabaseHelper`](lib/database_helper.dart) ใช้ **singleton pattern** อย่างถูกต้องสำหรับการเชื่อมต่อฐานข้อมูล

---

## 🚀 แผนงานอนาคต (แผนฟื้นฟู)

### 🏃 ระยะสั้น: ชัยชนะด่วน (1–2 Sprints)

| # | สิ่งที่ต้องทำ | ผลกระทบ |
|---|-------------|---------|
| 1 | **Hash รหัสผ่านทั้งหมด** ด้วย `bcrypt` หรือ `argon2` ย้ายรหัสผ่านข้อความธรรมดาที่มีอยู่เมื่อเข้าสู่ระบบครั้งแรก | 🔒 แก้ไขด้านความปลอดภัยระดับวิกฤต |
| 2 | **ลบรหัสผ่านออกจาก `user-stat.json`** ใช้ token ที่ปลอดภัย หรือ `flutter_secure_storage` สำหรับการคงสถานะ session | 🔒 แก้ไขด้านความปลอดภัยระดับวิกฤต |
| 3 | **แก้ไข timezone ที่ถูกฝังตาย** ใช้ `FlutterTimezone.getLocalTimezone()` อย่างสม่ำเสมอ (import ไว้แล้วใน [`nortification_service.dart`](lib/nortification_service.dart) แต่ไม่ได้ใช้ใน [`nortification_next.dart`](lib/nortification_next.dart)) | 🐛 แก้บั๊ก |
| 4 | **แก้คำผิดในชื่อไฟล์และ class**: `nortification` → `notification`, `carlendar` → `calendar`, `edit_Profile` → `edit_profile` ใช้สคริปต์เปลี่ยนชื่อ + IDE refactor | 🧹 ความเป็นระเบียบของโค้ด |
| 5 | **เพิ่ม `.gitignore` สำหรับ `android/build/`** โฟลเดอร์ `android/build/reports/` ถูก commit เข้า version control พร้อมรายงาน HTML 660+ บรรทัด | 🧹 ความเป็นระเบียบของ repo |
| 6 | **เก็บรูปภาพเป็นไฟล์บนดิสก์** อ้างอิงด้วย path ในฐานข้อมูลแทน Base64 blobs ย้ายข้อมูลที่มีอยู่ | ⚡ ประสิทธิภาพ |

### 🏗️ ระยะกลาง: การปรับปรุงให้ทันสมัย (1–3 เดือน)

| # | สิ่งที่ต้องทำ | ผลกระทบ |
|---|-------------|---------|
| 7 | **นำระบบจัดการ state เข้ามา** — Riverpod 2.0 หรือ Bloc แยก business logic ออกจาก widgets ไปเป็น providers/cubits | 🏗️ สถาปัตยกรรม |
| 8 | **สร้างชั้นข้อมูลที่เหมาะสม**: `models/`, `repositories/`, `services/` กำหนด Dart data classes ด้วย `freezed` + `json_serializable` | 🏗️ สถาปัตยกรรม |
| 9 | **ยกเลิกการเก็บข้อมูลแบบไฟล์ JSON** รวมข้อมูลทั้งหมดไว้ใน SQLite ใช้ `shared_preferences` หรือตาราง SQLite สำหรับการตั้งค่าแอป | 🏗️ ความสมบูรณ์ของข้อมูล |
| 10 | **เพิ่ม `flutter_intl` / `intl`** สำหรับ localization แยก string ภาษาไทยทั้งหมดไปไฟล์ `.arb` เพิ่มภาษาอังกฤษเป็นภาษาที่สอง | 🌍 การเข้าถึง |
| 11 | **เข้ารหัสฐานข้อมูล** ด้วย `sqflite_sqlcipher` หรือ `drift` พร้อมการเข้ารหัส | 🔒 ความปลอดภัย |
| 12 | **เขียน test**: Unit tests สำหรับ business logic, widget tests สำหรับ forms, integration tests สำหรับ flow NFC เป้าหมาย coverage 70%+ | ✅ ความน่าเชื่อถือ |
| 13 | **เพิ่ม CI/CD**: GitHub Actions สำหรับ `flutter analyze`, `flutter test`, และ build APK/IPA อัตโนมัติ | 🚀 DevOps |
| 14 | **เปลี่ยนการ parse NFC payload ด้วยมือ** เป็นรูปแบบที่มีโครงสร้าง (JSON หรือ Protobuf) พร้อมการกำหนดเวอร์ชัน schema | 🏗️ ความสามารถในการบำรุงรักษา |

### 🔭 ระยะยาว: วิสัยทัศน์ (3–12 เดือน)

| # | สิ่งที่ต้องทำ | ผลกระทบ |
|---|-------------|---------|
| 15 | **Cloud Sync และรองรับหลายอุปกรณ์**: เพิ่ม backend (Firebase / Supabase / API กำหนดเอง) สำหรับซิงค์ข้ามอุปกรณ์ โทรศัพท์ผู้ดูแลและโทรศัพท์ผู้ป่วยควรซิงค์กัน | ☁️ ขยายผลิตภัณฑ์ |
| 16 | **แดชบอร์ดผู้ดูแล (เว็บ)**: พอร์ทัลเว็บให้ผู้ดูแลตรวจสอบการปฏิบัติตามการกินยาของผู้พึ่งพิงทั้งหมดจากระยะไกล | 📊 ขยายผลิตภัณฑ์ |
| 17 | **ระบบตรวจสอบปฏิสัมพันธ์ยาด้วย AI**: เชื่อมต่อฐานข้อมูลปฏิสัมพันธ์ยา (เช่น RxNorm, OpenFDA) เพื่อเตือนเมื่อยาที่เพิ่มใหม่ขัดแย้งกับยาที่มีอยู่ | 🤖 ฟีเจอร์ AI |
| 18 | **การเตือนอัจฉริยะด้วย ML**: ใช้ on-device ML เพื่อเรียนรู้รูปแบบการกินยาจริงของผู้ป่วย และปรับเวลาเตือนให้เหมาะสม (เช่น "ผู้ป่วยนี้กินยาเช้าตอน 7:30 เสมอ ไม่ใช่ 6:00") | 🤖 ฟีเจอร์ AI |
| 19 | **เชื่อมต่อกล่องจ่ายยา Bluetooth**: ร่วมมือกับผู้ผลิตฮาร์ดแวร์เพื่อเชื่อมต่อกับกล่องจ่ายยาอัจฉริยะผ่าน BLE ทดแทนแท็ก NFC | 🔌 การเชื่อมต่อ IoT |
| 20 | **ส่งออกข้อมูลสุขภาพ**: สร้างรายงาน PDF/CSV เรื่องการปฏิบัติตามการกินยาเพื่อแชร์กับแพทย์ เชื่อมต่อกับ Apple Health / Google Fit | 📋 ประโยชน์ทางคลินิก |
| 21 | **ตรวจสอบ Accessibility**: เพิ่มรองรับ screen reader, โหมดคอนทราสต์สูง, และพื้นที่สัมผัสที่ใหญ่ขึ้นสำหรับผู้สูงอายุ | ♿ การเข้าถึง |
| 22 | **ย้ายไปใช้ Drift (เดิมชื่อ Moor)**: แทนที่ `sqflite` ดิบด้วย `drift` สำหรับ database queries ที่ type-safe, reactive พร้อมรองรับ migration | 🏗️ ปรับปรุง DX |

---

## 📁 โครงสร้างโปรเจกต์

```
lib/
├── main.dart                    # จุดเริ่มต้น, หน้าเข้าสู่ระบบ, การเริ่มต้นแอป
├── database_helper.dart         # SQLite singleton (users, medicines, calendar_alerts, eated)
├── view_dashboard.dart          # ศูนย์กลาง — มุมมองโดสรายวัน, สแกน NFC, ทำเครื่องหมายกินแล้ว
├── view_carlendar.dart          # มุมมองปฏิทินรายสัปดาห์
├── view_menu.dart               # เมนูลิ้นชัก/การนำทาง
├── add_carlendar.dart           # สร้างการเตือนใหม่ (+ เขียน NFC)
├── edit_carlendar.dart          # แก้ไขการเตือนที่มีอยู่ (+ เขียน NFC ใหม่)
├── add_medicine.dart            # เพิ่มยาใหม่ลงฐานข้อมูล
├── edit_medicine.dart           # แก้ไขยาที่มีอยู่
├── manage_medicine.dart         # รายการ/เรียงลำดับ/ลบยา
├── create_profile.dart          # สร้างโปรไฟล์ย่อย (ผู้พึ่งพิง)
├── edit_Profile.dart            # แก้ไขโปรไฟล์ย่อย
├── manage_profile.dart          # รายการ/แก้ไข/ลบโปรไฟล์
├── create_account.dart          # ลงทะเบียนผู้ใช้ใหม่
├── edit_account.dart            # แก้ไขการตั้งค่าบัญชี
├── forgotPassword.dart          # กู้รหัสผ่านด้วยคำถามกันลืม
├── nortification_service.dart   # Logic การแจ้งเตือนหลัก (หาโดสถัดไป, ตั้งเวลาแจ้งเตือน OS)
├── nortification_next.dart      # ตั้งเวลาแจ้งเตือนล่วงหน้า 2 วันแบบ batch
└── nortification_setting.dart   # UI สำหรับตั้งค่าเสียง/เลื่อนเตือน
```

---

## 📄 สัญญาอนุญาต (License)

ยังไม่ได้ระบุ กรุณาเพิ่มไฟล์ `LICENSE` ก่อนเปิดเป็น open source

---

## 🤝 การมีส่วนร่วม

โปรเจกต์นี้อยู่ในช่วง **การทำให้เสถียร** ก่อนที่จะเพิ่มฟีเจอร์ใหม่ ควรให้ความสำคัญกับรายการระยะสั้นในแผนงานด้านบน โดยเฉพาะการแก้ไขด้านความปลอดภัย (รายการที่ 1–2)

---

*README นี้ถูกสร้างขึ้นเป็นเอกสารส่งมอบเชิงกลยุทธ์ สะท้อนสถานะของ codebase ณ commit ล่าสุด และมีเจตนาเพื่อเป็นแนวทางให้ทีมพัฒนาชุดต่อไป*