name: pillmate
description: "Pillmate – Your Health, Your Reminder. A Flutter medication management app with NFC support."
publish_to: 'none'

version: 2.0.0+1

environment:
  sdk: ^3.11.0-90.0.dev

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # UI
  cupertino_icons: ^1.0.8

  # Storage & Database
  sqflite: ^2.3.0
  sqflite_common_ffi: ^2.3.0
  path: ^1.9.0
  path_provider: ^2.1.3
  shared_preferences: ^2.2.2
  flutter_secure_storage: ^9.0.0

  # Security
  crypto: ^3.0.3

  # NFC
  flutter_nfc_kit: ^3.6.0
  ndef: ^0.3.0

  # Notifications
  flutter_local_notifications: ^18.0.1
  timezone: ^0.10.0
  flutter_timezone: ^3.0.1

  # Media
  image_picker: ^1.0.0
  audioplayers: ^6.0.0

  # State Management
  flutter_riverpod: ^2.4.9

  # Utilities
  fluttertoast: ^8.2.4
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/simpleProfile/
    - assets/pill/
    - assets/db/user
    - assets/db/appstatus.json
    - assets/sound_norti/a06_clock_alarm_continue_1_30_min.mp3
    - assets/sound_norti/a05_clock_alarm_continue_1_min.mp3
    - assets/sound_norti/a04_clock_alarm_continue_30_sec.mp3
    - assets/sound_norti/a03_clock_alarm_normal_1_30_min.mp3
    - assets/sound_norti/a02_clock_alarm_normal_1_min.mp3
    - assets/sound_norti/a01_clock_alarm_normal_30_sec.mp3